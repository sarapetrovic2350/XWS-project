import { RatingAccommodation } from './rating-accommodation.model';

describe('RatingAccommodation', () => {
  it('should create an instance', () => {
    expect(new RatingAccommodation()).toBeTruthy();
  });
});
