import { RatingAccommodationByGuest } from './rating-accommodation-by-guest.model';

describe('RatingAccommodationByGuest', () => {
  it('should create an instance', () => {
    expect(new RatingAccommodationByGuest()).toBeTruthy();
  });
});
