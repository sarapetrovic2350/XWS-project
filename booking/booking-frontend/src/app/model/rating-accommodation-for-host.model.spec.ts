import { RatingAccommodationForHost } from './rating-accommodation-for-host.model';

describe('RatingAccommodationForHost', () => {
  it('should create an instance', () => {
    expect(new RatingAccommodationForHost()).toBeTruthy();
  });
});
